# Environment variables for Media Compression API

# Required: API keys for authentication (comma-separated)
API_KEYS=your_api_key_here,another_api_key

# Required: Upstash QStash for queue management
UPSTASH_QSTASH_REST_URL=https://your-qstash-rest-url
UPSTASH_QSTASH_TOKEN=your-qstash-token

# Required: Vercel Blob for file storage
BLOB_READ_WRITE_TOKEN=your-vercel-blob-token

# Required: Vercel KV for rate limiting and job state
KV_REST_API_URL=https://your-kv-rest-url
KV_REST_API_TOKEN=your-kv-token

# Optional: Configuration
MAX_FILE_SIZE=524288000
TIMEOUT=300000

# Optional: Webhook notifications
WEBHOOK_SECRET=your-webhook-secret
